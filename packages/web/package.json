{
  "name": "@tubewhisperer/web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:widgets": "esbuild src/tw-summary.ts --bundle --format=esm --outfile=dist/tw-summary.js && esbuild src/tw-replies.ts --bundle --format=esm --outfile=dist/tw-replies.js",
    "build": "node build.js && pnpm run build:widgets",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
