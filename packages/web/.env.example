# Backend API URL
# Use relative path for monorepo deployment (frontend and backend on same domain)
# In production: /api routes to backend serverless functions
# In development: Vite proxy will forward /api to http://localhost:3000/api
VITE_API_URL=/api
