{
  "buildCommand": "pnpm install && pnpm --filter @vocalytics/web build && cp -r packages/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...path]" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
